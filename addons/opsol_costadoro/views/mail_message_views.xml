<odoo>

	<!-- <record id="view_sale_reman_form" model="ir.ui.view">
        <field name="name">sale.order.line.form</field>
        <field name="model">sale.order.line</field>
        <field name="arch" type="xml">
            <form string="PIECES REMAN">
                <field name="is_reman" invisible="1"/>
                <field name="company_id" invisible="1"/>
                <header>
                    <field name="reman_state" widget="statusbar" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box"/>
                    <div class="oe_title">
                        <label for="product_id"/>
                        <h1><field name="product_id" options="{'line_breaks': False}" placeholder="Article"/></h1>
                        <label for="order_id"/>
                        <h3><field name="order_id" options="{'line_breaks': False}" placeholder="Commande"/></h3>
                    </div>
                    <group>
                        <group>
                            <field name="name" />
                        </group>
                        <group>
                            
                        </group>
                    </group>
                    <notebook>
                        <page string="Order" name="reman_order">
                            <group>
                                <group string="Commande">
                                    <field name="order_id" options="{'no_create': True}" />
                                    <field name="date_order" />
                                    <field name="order_partner_id"/>
                                </group>
                                <group string="Stock">
                                    <field name="product_uom_qty" string="Qty Order"/>
                                </group>
                            </group>
                        </page>
                        <page string="RETURN" name="page_return">
                            <group string="Commande">
                                <field name="return_n_type" />
                                <field name="return_n_qty" />
                            </group>
                            <group name="Stock">
                            </group>
                        </page>
                        <page string="INVOICE" name="page_invoice">
                        </page>
                        <page string="CREDIT NOTE" name="page_credit_note">
                        </page>
                        <page string="BALANCE" name="page_balance">
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" options="{'open_attachments': True}"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_sale_reman_filter" model="ir.ui.view">
        <field name="name">sale.order.line.lead</field>
        <field name="model">sale.order.line</field>
        <field name="arch" type="xml">
            <search string="Search REMAN">

                <field name="product_id" />
                <field name="order_id" string="SALE ORDER N°" />
                <field name="project_id" string="JOB/PROJET" />
                <field name="order_partner_id" string="Bateau" />
                <field name="reman_state"/>
                <separator />
                <group expand="0" string="Group By">
                    <filter string="ETAT" name="groupby_reman_state" context="{'group_by':'reman_state'}"/>
                </group>

            </search>
        </field>
    </record>

    <record id="view_sale_reman_kanban" model="ir.ui.view">
        <field name="name">sale.order.line.kanban</field>
        <field name="model">sale.order.line</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" sample="1" quick_create="false">
                <field name="name"/>
                <field name="product_id"/>
                <field name="order_id"/>
                <field name="date_order"/>
                <field name="reman_state"/>
                <field name="currency_id"/>
                <field name="order_partner_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_card oe_kanban_global_click">
                            <div class="o_kanban_record_top mb16">
                                <div class="o_kanban_record_headings mt4">
                                    <strong class="o_kanban_record_title">
                                        <span t-out="record.product_id.value"/>
                                    </strong>
                                </div>
                                <strong>
                                    <field name="price_total" widget="monetary"/>
                                </strong>
                            </div>
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_right">
                                    <field name="reman_state"
                                        widget="label_selection"
                                        options="{'classes': {'draft': 'info', 'cancel': 'default', 'sale': 'success'}}"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record> -->

    <record id="view_mail_message_tree_opsol" model="ir.ui.view">
        <field name="name">mail.message.tree</field>
        <field name="model">mail.message</field>
        <field name="arch" type="xml">
            <tree string="PIECES REMAN" editable="bottom" create="0" delete="0">
                <field name="date"/>
                <field name="author_id"/>
                <!-- <field name="subject"/> -->
                <field name="body"/>
            </tree>
        </field>
    </record>

    <record id="action_mailmsg_reman" model="ir.actions.act_window">
        <field name="name">Activites</field>
        <field name="res_model">mail.message</field>
        <field name="view_mode">tree</field>
        <field name="view_id" ref="opsol_ajmarine.view_mail_message_tree_opsol"/>
        <!-- <field name="search_view_id" ref="opsol_ajmarine.view_mail_message_tree_opsol"/> -->
        <!-- <field name="context">{'search_default_groupby_reman_state': 1}</field> -->
        <field name="domain">[('model', '=', 'sale.order.line'),('body', '!=', False)]</field>
        <field name="help" type="html">
          <p></p>
        </field>
    </record>

    <menuitem id="menu_reman_root_remaudit" name="REMAN Audit" action="action_mailmsg_reman" parent="menu_reman_root" sequence="30" />

</odoo>