<odoo>

	<record id="project_task_action_fsm_opsol" model="ir.actions.act_window">
        <field name="name">Interventions</field>
        <field name="res_model">project.task</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="search_view_id" ref="industry_fsm.project_task_view_search_fsm"/>
        <field name="domain">[('is_fsm', '=', True), ('project_id', '!=', False), ('display_in_project', '=', True)]</field>
        <field name="context">{
            'fsm_mode': True,
            'search_default_my_tasks': True,
            'search_default_planned_future': True,
            'search_default_planned_today': True,
            'default_user_ids': [(4, uid)],
            'default_scale': 'day',
        }</field>
        <field name="help" type="html">
            <p class='o_view_nocontent_smiling_face'>No tasks found. Let's create one!</p>
            <p>Find here your upcoming tasks for the next few days.</p>
        </field>
    </record>

    <record id="project_task_view_list_fsm_opsol" model="ir.ui.view">
        <field name="name">project.task.tree.fsm.opsol</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="industry_fsm.project_task_view_list_fsm"/>
        <field name="arch" type="xml">
        
            <xpath expr="//field[@name='user_ids']" position="after">
                <field name="quick_equipment_ids" widget="many2many_tags" optional="show"/>
                <field name="types_equip_ids" widget="many2many_tags" optional="show" />
                <field name="model_equip_ids" widget="many2many_tags" optional="show" />
                <field name="manufacturer_ids" widget="many2many_tags" optional="show" />
            </xpath>

        </field>
    </record>

    <!-- <record id="action_line_metric" model="ir.actions.act_window">
        <field name="name">Interventions</field>
        <field name="res_model">opsol_costadoro.line_metric</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
          <p>Creez une nouvelle metrique equipement.</p>
        </field>
    </record> -->

    <menuitem 
        id="menu_client_equipment_inters"
        name="Interventions"
        action="project_task_action_fsm_opsol"
        parent="client_equipment.menu_client_equipment"
        sequence="17" />

</odoo>