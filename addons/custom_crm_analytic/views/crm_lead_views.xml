<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="crm_lead_view_form_inherit_analytic" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit.analytic</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet/notebook" position="inside">
                <page name="analytic_tab" string="Analytique">
                    <field name="analytic_account_ids">
                        <list string="Comptes Analytiques">
                            <field name="name"/>
                            <field name="company_id"/>
                        </list>
                    </field>
                    <group>
                        <field name="analytic_account_id"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
```

## 🔑 Changements appliqués

1. **Ligne 10** : Supprimé `mode="tree,kanban"` (pas nécessaire en Odoo 18)
2. **Ligne 11** : `<tree>` → `<list>`
3. **Ligne 14** : `</tree>` → `</list>`

---

## 📂 Quel module ?

Ce fichier appartient probablement à :
```
/mnt/extra-addons/custom_crm_analytic/views/crm_lead_views.xml