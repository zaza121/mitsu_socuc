<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_template_form_view_invoice_policy_inherit_sale_project_op" model="ir.ui.view">
        <field name="name">product.template.form.mitsu</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="sale_project.product_template_form_view_invoice_policy_inherit_sale_project"/>
        <field name="arch" type="xml">
            
            <field name="service_policy" position="before">
                <field name="temp_task_ids" invisible="type != 'service' or sale_ok == False" widget="many2many_tags" domain="[('project_temp_id', '=', project_template_id)]"/>
            </field>

        </field>
    </record>

</odoo>
